name: sema_jittor_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - jittor>=1.3.8
      - timm==0.9.8
      - numpy>=1.24.0
      - scipy
      - tqdm
      - easydict
      - Pillow
      - torchvision
